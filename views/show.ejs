<% include partials/header %>
<div class="ui main text container segment">
    <div class="ui huge header"><%= readBlog.title %></div>
    <div class="ui top attached">
        <div class="item">
            <img class="ui centered rounded image" src="<%= readBlog.img %>">
            <div class="content">
                <p><%= readBlog.created.toDateString() %></p>
            </div>
            <div class="description">
                <p><%= readBlog.body %></p>
            </div>
            <a href="/blogs/<%= readBlog._id %>/edit" class="ui inverted purple button">Edit</a>    
        </div>
    </div>
</div>
<% include partials/footer %>
