<h1>Welcome to blogs</h1>

<% blogs.forEach((blog) => { %>
    <h1><%= blog.title %></h1>
    <img src="<%= blog.img %>" alt="">
    <p><%= blog.body %></p>
    <p><%= blog.created %></p>
    <hr>
<% }) %>
